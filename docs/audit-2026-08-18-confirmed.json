{
 "confirmed": [
  {
   "device": "wiphone",
   "value": "med",
   "risk": "low",
   "effort": "small",
   "lens": "wp-power",
   "title": "SX1276 IRQ register is read over bit-banged SPI every loop pass",
   "file": "/Users/nickhowe/wiphone/WiPhone/mesh_phy.cpp",
   "line": 173,
   "claim": "meshService.loop() (WiPhone.ino:2713) calls meshPhy.poll() every main-loop pass, and poll() starts with readReg(REG_IRQ_FLAGS) (mesh_phy.cpp:169-176) over RHSoftwareSPI, a bit-banged transport (mesh_phy.cpp:134-136): each read is ~64 GPIO operations plus per-bit delays, roughly 50-100 us at the 80 MHz idle clock. With CONFIG_FREERTOS_HZ=1000 (confirmed in docs/HANDOFF.md:1132) the loop runs ~1000 passes/s, so this one register read alone keeps the core out of WAITI for ~5-10% of idle time. DIO0 is wired and configured as RxDone but explicitly unused ('DIO0 = RxDone (unused; we poll)', mesh_phy.cpp:100). Safe fix without touching RX semantics: rate-limit the SPI poll to every ~10 ms (a LongFast SF11/250k packet is >100 ms on air and RX_DONE latches until cleared, so nothing can be missed); gating on the DIO0 GPIO instead would be cheaper still but needs the pin wiring verified on hardware first.",
   "change": "mesh_phy.cpp:169 (MeshPhy::poll) \u2014 add a monotonic rate limit before the SPI read: keep a member uint32_t lastPollMs; at the top of poll(), after the ready check, `if ((uint32_t)(millis() - lastPollMs) < 10) return false; lastPollMs = millis();`. (Equivalently, guard the meshPhy.poll() call at meshtastic_service.cpp:557.) Do NOT switch to DIO0 gating in this change \u2014 that needs the pin wiring verified on hardware first.",
   "reasoning": "Claim verified by reading the code. MeshtasticService::loop() (meshtastic_service.cpp:508, live because MESHTASTIC_PHY is defined at config.h:50) calls meshPhy.poll() unconditionally at meshtastic_service.cpp:557; it is invoked from WiPhone.ino:2718 every superloop pass, and the loop is paced by vTaskDelay(1) (WiPhone.ino:2908) with CONFIG_FREERTOS_HZ=1000 (docs/HANDOFF.md:1132), i.e. ~1000 polls/s at idle. poll() begins with readReg(REG_IRQ_FLAGS) (mesh_phy.cpp:173) over RHSoftwareSPI bit-banged SPI (mesh_phy.cpp:134-136) \u2014 16 clocked bits plus CS toggles per pass; DIO0 is mapped to RxDone but explicitly unused (mesh_phy.cpp:100). The safety argument holds: configureLongFast() sets SF11/250kHz (mesh_phy.cpp:114-126), symbol time 8.19 ms, so the 16-symbol preamble alone is ~131 ms \u2014 no second packet can complete within 10 ms of the first in RX_CONTINUOUS, and RX_DONE latches until the write-1-to-clear at mesh_phy.cpp:178, so a 10 ms poll cadence cannot lose a packet and adds at most 10 ms RX latency. TX (send(), mesh_phy.cpp:205) polls its own flags synchronously and is unaffected. The change is a few self-contained lines with no semantic change to RX handling \u2014 safe on a working device. Minor nits: the call site is WiPhone.ino:2718, not 2713, and polling is already skipped while the GBC emulator is active; neither affects the finding."
  },
  {
   "device": "wiphone",
   "value": "high",
   "risk": "med",
   "effort": "medium",
   "lens": "wp-power",
   "title": "Out-of-range WiFi: hard reconnect cycle every 20 s plus perpetual driver auto-reconnect between attempts",
   "file": "/Users/nickhowe/wiphone/WiPhone/WiPhone.ino",
   "line": 1980,
   "claim": "When disconnected, the loop calls wifiState.connectToPreferred() every WIFI_RETRY_PERIOD_MS=20 s (config.h:72) forever, and the code's own comment says connectToWiFi() hard-cycles the radio (disconnect(true)+begin, WiPhone.ino:1976-1979). Worse, Networks.cpp:519-523 documents that between our retries the Arduino driver auto-reconnects perpetually on 'AP not found', keeping the radio mid-association until the autoswitch scanner's WiFi.disconnect() interrupts it. So a phone carried out of range all day (the September hunt) keeps the WiFi radio actively probing/associating most of the time - likely the single largest field battery drain, dwarfing everything in the loop. The codebase already has the exact backoff pattern needed: _discScans in autoSwitchTick eases scans from 2 min to 5 min after ~10 min of failures and resets on connect/screen-wake (Networks.cpp:494-514). Mirroring that for the reconnect retry (ease 20 s to 2-5 min after sustained absence, immediate retry on screen wake, and a WiFi.disconnect() to quiesce the driver between backed-off attempts) costs nothing in reachability - with no AP in range the phone is SIP-unreachable regardless and LoRa is unaffected. Needs care with the doReconnect/userDisabled flags and the gbcXfer/scanBusy gates on line 1980, hence risk med.",
   "change": "In WiPhone.ino around line 1985 (plus a small helper in Networks.cpp/h): add a disconnected-retry counter mirroring _discScans \u2014 keep the first ~5 retries at WIFI_RETRY_PERIOD_MS (20 s), then ease the retry period to 2-5 min; reset the counter on successful connect (SYSTEM_EVENT_STA_GOT_IP / setConnected(true)) and force an immediate retry on screen wake (same wake edge autoSwitchTick already computes at Networks.cpp:440-441); while in the eased state, issue a plain WiFi.disconnect() after a failed attempt (the same quiesce used at Networks.cpp:523) so the driver's auto-reconnect loop does not keep the radio mid-association between attempts. Leave all existing gates on line 1985 (gbcXferOn, scanBusy, doReconnect, userDisabled) untouched.",
   "reasoning": "Every factual claim checks out against the code: WiPhone.ino:1985 retries connectToPreferred() every 20 s (config.h:72) with no backoff whenever disconnected; connectToWiFi() hard-cycles the radio (WiFi.disconnect(true) at Networks.cpp:80, WiFi.begin at :111); Networks.cpp:517-524 documents the driver's perpetual auto-reconnect on 'AP not found' and the plain WiFi.disconnect() that quiesces it; and the _discScans backoff pattern with connect-reset and screen-wake fast path exists at Networks.cpp:498-514. Safety: the change mirrors a field-proven pattern in the same file, and reconnection retains an independent second path \u2014 autoSwitchEvaluate() calls connectTo() directly (Networks.cpp:597), autoswitch defaults enabled (:416-428) and scans immediately on screen wake \u2014 so a backoff bug cannot strand the phone offline unless the user set autosw=0, and even then the eased 2-5 min period bounds worst-case rejoin to the same window the owner already accepted for scans. Med risk is fair (it touches connectivity gates on a deployed device) and within scope; value is high for the September all-day out-of-range use case, which the codebase's own comments treat as a first-class scenario."
  },
  {
   "device": "wiphone",
   "value": "high",
   "risk": "med",
   "effort": "small",
   "lens": "wp-power",
   "title": "Idle main loop still does real work at ~1 kHz; the existing 'busy' predicate could stretch the tick when nobody is watching",
   "file": "/Users/nickhowe/wiphone/WiPhone/WiPhone.ino",
   "line": 2903,
   "claim": "The loop correctly ends in vTaskDelay(1) (WiPhone.ino:2890-2903), so the idle task runs - that fix is in. But every 1 ms pass still executes sip.checkCall() with connected()/stale()/available() checks on up to 4 sockets plus sip.checkMessage() (2313-2331, 2649), the bit-banged mesh SPI poll (2713), smsMirrorPollLoop's gate checks, the stale-key sweep, and a dozen timer comparisons - order 100-300 us per pass at 80 MHz, i.e. the core is awake 10-30% of idle time. The loop already computes exactly the right predicate at 2876-2881 (screen off, no game, no transfer, no music, !sipNeedsFullSpeed) for the CPU-MHz gate; reusing that same bool to choose vTaskDelay(5) when idle would cut pass rate 5x with a worst-case +4 ms latency on keypad wake (interrupt-latched), mesh RX (packets >100 ms on air), and SIP socket servicing (well under network jitter). Caveats a careful engineer must respect: while smsMirrorPollLoop is mid-transfer (s_state != PS_IDLE) its one-step-per-pass state machine would slow 5x, so either include 'mirror poll active' in busy or accept slower 2-minute polls; and the mesh vibro/pop timers (180/280 ms) get +-4 ms jitter, imperceptible. Marked med risk only because it changes the cadence of everything at once and deserves a soak test; it does not touch call/audio timing (any SIP state in sipNeedsFullSpeed keeps the 1 ms tick).",
   "change": "WiPhone.ino:2880-2908 \u2014 hoist the existing `busy` bool out of the CPU-gate block scope (it is currently local to the `{}` at 2880), leave the frequency gate exactly as is, and change line 2908 from `vTaskDelay(1)` to `vTaskDelay(busy ? 1 : 5)`. No predicate changes: sipNeedsFullSpeed() already keeps the 1 ms tick for any ringing/live/imminent call, and the mirror poll's stall-based deadline (sms_mirror_poll.cpp:418-419) already tolerates the 5x slower drain. Soak-test on the bench (idle overnight + one incoming call + one mirrored text + one mesh message) before calling it done.",
   "reasoning": "Claim verified by reading the code. The loop ends in vTaskDelay(1) (WiPhone.ino:2908) so passes run at ~1 kHz, and each idle pass really does the cited work: TinySIP::checkCall (tinySIP.cpp:1715) checks connected()/stale() on the proxy and available() on up to 4 connections every call (gated at WiPhone.ino:2291 by wifi+account+sipMayPoll, all true on an idle registered phone); sip.checkMessage at 2654; meshService.loop() at 2718 \u2192 MeshPhy::poll (mesh_phy.cpp:169) bit-bangs a software-SPI register read every pass; smsMirrorPollLoop at 1578; audio->loop() at 2822; stale-key sweep at 1654. The busy predicate at 2881-2885 is exactly the right gate: any ringing/live/imminent call is in sipNeedsFullSpeed() (1436-1448) so call/audio timing keeps the 1 ms tick; keypad, power-button and headphone events are interrupt-latched (2725-2760) so wake latency is bounded at +4 ms; the Game Boy never reaches this line. The finding's mirror-poll caveat checks out as tolerable without extra gating: 512 B/pass (sms_mirror_poll.cpp:32) at ~200 Hz is ~100 KB/s, the stall deadline re-arms on any received bytes (sms_mirror_poll.cpp:418-419), and TOTAL_BUDGET_MS=120 s is not threatened. Mesh pop/vibro while idle already run outside the busy set today (at 80 MHz) and audio buffers ~24 ms ahead, so 5 ms servicing is fine. Med risk grading is honest \u2014 it changes idle cadence of everything at once and needs a soak \u2014 but the change is small, reuses an already-proven predicate, and worst case costs 4 ms of latency nobody can perceive. Within the session's risk budget and a careful engineer would make it."
  },
  {
   "device": "wiphone",
   "value": "med",
   "risk": "med",
   "effort": "small",
   "lens": "wp-power",
   "title": "SIP Idle-state poll runs every pass, not at 50 ms as believed",
   "file": "/Users/nickhowe/wiphone/WiPhone/WiPhone.ino",
   "line": 2313,
   "claim": "There is no 50 ms gate anywhere on the SIP poll: in CallState::Idle the do/while sip.checkCall(now) loop at WiPhone.ino:2317-2331 runs on every single main-loop pass (~1000x/s), each call checking tcpProxy->connected()/stale() and available() on up to four Connection objects (tinySIP.cpp checkCall body), plus registrationValid() and a separate sip.checkMessage() at 2649. That is roughly 10-30 us per pass, a few percent of idle CPU. Rate-limiting only the Idle/registered state to a 50 ms cadence (keeping every in-call state per-pass) adds at most 50 ms to incoming-INVITE detection - far below SIP retransmission tolerances - but because it sits on the inbound-call reachability path it should only land alongside an owner-assisted inbound-call test, which is why it is graded med risk despite being mechanically trivial. Note the fully redundant alternative: if the adaptive-tick finding lands, this one becomes mostly moot and can be skipped.",
   "change": "WiPhone.ino:2318 \u2014 in the CallState::Idle branch only, add a static timestamp gate (e.g. static uint32_t msLastIdleSipPoll; if (!elapsedMillis(now, msLastIdleSipPoll, 50)) skip the branch, else update it) around the checkCall do/while (2322-2336) and registrationValid/redraw logic (2337-2352); leave checkMessage at 2654 and all non-Idle states ungated. Land only alongside an owner-assisted inbound-call test, and skip entirely if the adaptive-tick finding is applied instead.",
   "reasoning": "Claim verified by reading the code. (a) Factually right: sipMayPoll() (WiPhone.ino:1419-1434) is a Game-Boy-memory gate, not a rate gate \u2014 there is no 50 ms cadence anywhere; the main loop runs at ~1000 Hz (vTaskDelay(1) at WiPhone.ino:2908); in CallState::Idle the do/while at WiPhone.ino:2322-2336 calls sip.checkCall(now) every pass, and checkCall (tinySIP.cpp:1715) checks tcpProxy connected()/stale() (line 1720) plus available() on up to four Connection objects (lines 1739-1749); registrationValid(now) runs per-pass at 2337 and sip.checkMessage per-pass at 2654 (finding cited 2649 \u2014 trivial drift). (b) Safe at its stated med-risk grade with its stated safeguard: SIP is over TCP so a 50 ms cadence only delays reading LwIP-buffered data; 50 ms is far inside SIP T1=500 ms retransmit tolerance and irrelevant to the 60 s registration expiry; the change scope correctly keeps every in-call/teardown state per-pass. It touches inbound-call reachability, so it must land only with the owner-assisted inbound-call (and ideally inbound-MESSAGE) test the finding already requires. Caveats that don't overturn: battery win is smaller than the CPU-duty win since the loop still wakes at 1 kHz regardless, and the finding is correct that an adaptive-tick change would subsume this one \u2014 skip if that lands first."
  },
  {
   "device": "wiphone",
   "value": "high",
   "risk": "low",
   "effort": "small",
   "lens": "wp-stability",
   "title": "SIP send path: vsprintf into 500-byte stack buffer, remote-influenced content",
   "file": "/Users/nickhowe/wiphone/WiPhone/tinySIP.h",
   "line": 247,
   "claim": "Connection::print() and Connection::printf() (tinySIP.h:243-267) format with vsprintf into a fixed char buffer[500] on the UI-task stack with no length limit. Call sites pass values parsed from inbound SIP traffic: TCP_PRINTF at tinySIP.cpp:3568 prints respRouteSet[i] (built from the peer's Record-Route headers), and 3583/3606/3611/3621/3625 print display-name + addr-spec + tag from the parsed To/From headers. The RX buffer admits messages up to MAX_MESSAGE_SIZE=2000 (tinySIP.h:538), so a single inbound INVITE/MESSAGE with a ~600+ char From display name or Record-Route value \u2014 which the stack must echo back in its response \u2014 overflows the stack frame. The listener is plain WiFiUDP on the LAN, so this is reachable by any LAN peer, not just the proxy. Fix is mechanical: vsnprintf(buffer, sizeof(buffer), ...) in both methods; normal VoIP.ms traffic never approaches 500 bytes so behavior in the field is unchanged.",
   "change": "tinySIP.h:247 and tinySIP.h:264 \u2014 replace vsprintf(buffer, format, aptr) with vsnprintf(buffer, sizeof(buffer), format, aptr) in both Connection::print() and Connection::printf().",
   "reasoning": "Verified by reading the code. tinySIP.h:243-269: both Connection::print() and Connection::printf() format with vsprintf into a fixed char buffer[500]={0} on the caller's stack, no length limit. Remote-influenced call sites confirmed in tinySIP.cpp: line 3568 prints respRouteSet[i] (built from peer Record-Route headers, populated during inbound parse near line 2436) and lines 3583/3606/3611/3621/3625 print remoteNameDyn/remoteUriDyn/remoteTagDyn, which derive from respToDispName/respFromDispName parsed off inbound To/From headers (tinySIP.cpp:369-377, extStrdup with no \u2264500 cap). The RX path admits messages up to MAX_MESSAGE_SIZE=2000 (tinySIP.h:538), so an inbound message with a ~600+ char display name or Record-Route value the stack must echo overflows the 500-byte stack frame; reachable by any LAN peer. Both conditions hold: the claim is factually right, and the implied change is safe on a working device \u2014 vsnprintf truncates instead of overflowing, and legitimate VoIP.ms traffic never approaches 500 bytes so field behavior is unchanged."
  },
  {
   "device": "wiphone",
   "value": "high",
   "risk": "low",
   "effort": "small",
   "lens": "wp-stability",
   "title": "SIP header parser writes past respHeaderName[100] with no cap check",
   "file": "/Users/nickhowe/wiphone/WiPhone/tinySIP.cpp",
   "line": 2389,
   "claim": "parseAllHeaders() does respHeaderName[respHeaderCnt++] = s (tinySIP.cpp:2389) and respHeaderValue[respHeaderCnt-1] = s (line 2414) with no comparison against MAX_HEADER_CNT=100 (tinySIP.h:540); both arrays are fixed char*[100] members (tinySIP.h:847-848). A minimal header line costs ~5 bytes ('a:b\\r\\n'), so a 2000-byte message can carry ~380 headers and overwrite pointers well past both arrays into the adjacent TinySIP members (remoteToFromDyn, respTo*, respFrom* at tinySIP.h:849+), i.e. memory corruption from any UDP packet on the LAN. Fix: bail out (or stop storing) when respHeaderCnt reaches MAX_HEADER_CNT \u2014 a 3-line guard that can never trigger on legitimate traffic (real SIP messages carry ~10-20 headers).",
   "change": "In tinySIP.cpp parseAllHeaders(), before the store at line 2389, cap the count: if (respHeaderCnt >= MAX_HEADER_CNT) break; (stop parsing further headers). This guards both the respHeaderName[respHeaderCnt++] write at line 2389 and the respHeaderValue[respHeaderCnt-1] write at line 2414.",
   "reasoning": "Verified against source. parseAllHeaders() stores parsed header pointers via respHeaderName[respHeaderCnt++] (tinySIP.cpp:2389) and respHeaderValue[respHeaderCnt-1] (tinySIP.cpp:2414) with no check against MAX_HEADER_CNT=100 (tinySIP.h:540). Both are fixed char*[100] members (tinySIP.h:847-848) immediately followed by remoteToFromDyn/respTo*/respFrom* pointer members (tinySIP.h:849+). The parse loop walks buff, sized MAX_MESSAGE_SIZE=2000 (tinySIP.h:538,828). A minimal well-formed header 'a:b\\r\\n' is 5 bytes and increments respHeaderCnt each time (malformed headers hit the skipCharLiteral colon check and abort, but well-formed ones do not), so a 2000-byte message produces ~380 stored entries, writing ~280 pointers past respHeaderName[100] into respHeaderValue[] and the adjacent member pointers \u2014 real memory corruption. The fix is safe: legitimate SIP messages carry ~10-20 headers, so a respHeaderCnt>=MAX_HEADER_CNT bail-out can never trigger on real traffic. Two minor framing errors in the finding that do not affect validity: this path is TCP (tcp->read, tinySIP.cpp:1770), not UDP; and a single message is bounded to 2000 bytes rather than unbounded \u2014 but 2000 bytes is far more than enough to exceed 100 headers. Value high (LAN-triggerable corruption), risk low (guard is inert on legitimate traffic), effort small."
  },
  {
   "device": "wiphone",
   "value": "med",
   "risk": "low",
   "effort": "small",
   "lens": "wp-stability",
   "title": "strlen(NULL) after unchecked strdup in TextInputWidget redraw (typing path)",
   "file": "/Users/nickhowe/wiphone/WiPhone/GUI.cpp",
   "line": 11644,
   "claim": "TextInputWidget::redraw() does char* dup = strdup(inputStringDyn + textOffset) then immediately if (fit<=strlen(dup)) with no NULL check (GUI.cpp:11643-11644); PasswordInputWidget::redraw() has the identical pattern at 11861-11862. strdup allocates from internal heap (always internal in this build), and this runs on EVERY redraw of a focused input \u2014 i.e. every keypress while composing a message \u2014 which is exactly the moment the ~20-30KB internal heap is most fragmented. If strdup returns NULL the phone dereferences it and reboots mid-typing. The sibling revealCursor() paths at 11606-11614 and 11825-11833 already wrap the same strdup in if (dup), so adding the guard matches existing intent; free(NULL) below is already safe. Two 1-line guards, no behavior change when memory is fine.",
   "change": "In /Users/nickhowe/wiphone/WiPhone/GUI.cpp change line 11644 from `if (fit<=strlen(dup)) {   // sanity check` to `if (dup && fit<=strlen(dup)) {   // sanity check` and line 11862 (PasswordInputWidget::redraw) identically from `if (fit<=strlen(dup)) {   // sanity check` to `if (dup && fit<=strlen(dup)) {   // sanity check`. The existing `free(dup)` calls at 11652 and 11870 remain correct (free(NULL) is a no-op).",
   "reasoning": "Claim verified by reading /Users/nickhowe/wiphone/WiPhone/GUI.cpp. TextInputWidget::redraw() line 11643 does `char* dup = strdup(inputStringDyn + textOffset);` and line 11644 immediately calls `if (fit<=strlen(dup))` with no NULL check; on strdup failure strlen(NULL) (or the subsequent `dup[fit]='\\0'` write at 11645) dereferences NULL and reboots the device. PasswordInputWidget::redraw() has the byte-identical pattern at 11861-11862 on outputStringDyn. Both run inside `if (this->focused)` on every redraw of a focused input, i.e. per keypress while composing \u2014 the moment internal heap pressure/fragmentation is highest, and strdup is plain-malloc internal heap in this build. The finding's supporting evidence also checks out: the sibling revealCursor() paths already wrap the same strdup in `if (dup)` (11606-11614 for text, 11825-11833 for password), so the guard matches the codebase's existing intent, and `free(dup)` at 11652/11870 is safe on NULL, so the guard introduces no leak or double-free. The change is safe on a working device: adding `dup &&` to each condition is a pure narrowing \u2014 behavior is bit-identical whenever strdup succeeds (the only path that executes today without crashing), and on failure the cursor is simply not drawn for that frame instead of rebooting. No state, timing, or layout is touched. Two one-line guards, small effort, low risk, genuine crash-prevention value."
  },
  {
   "device": "wiphone",
   "value": "med",
   "risk": "low",
   "effort": "small",
   "lens": "wp-stability",
   "title": "Early heap-step hypothesis A: notification pop reinstalls I2S driver twice per chime",
   "file": "/Users/nickhowe/wiphone/WiPhone/Audio.cpp",
   "line": 894,
   "claim": "Boot installs I2S at 16000 Hz mono (Audio.cpp:65-70). Audio::playPop() sets 8000 Hz (Audio.cpp:894), so configureI2S()'s cache (Audio.cpp:131-136) MISSES: the driver is uninstalled and reinstalled (~8 KB of internal DMA-capable RAM freed and re-taken, per the measured note at Audio.cpp:113-121), and restore() after the pop flips the rate back to 16000 for a SECOND uninstall/reinstall. So the first text-arrival chime after boot \u2014 precisely a first-half-hour event, correlating with the DROP line's sip/app context \u2014 churns two ~8 KB free/alloc cycles interleaved with SIP/mirror-poll allocations, a credible source of a one-time ~5 KB largest-block step (the same mechanism the code itself measured as 15,648\u21928,200 when music changed the rate). Evidence quality: mechanism confirmed in code + prior on-hardware measurement of the same class; the specific attribution to the logged steps is a hypothesis. Low-risk confirmation/fix: resample /pop.pcm to 16 kHz (or set the pop's rate to the boot rate) so the cache hits and the pop allocates nothing; or first just correlate the DROP timestamps with 'NOTIFY: buzz' log_e lines, which are already printed adjacent (WiPhone.ino:1536).",
   "change": "Diagnostic only, no code change: correlate the heap DROP log timestamps with the existing 'NOTIFY: buzz' log_e lines (WiPhone.ino:1536, adjacent to the playPop call at WiPhone.ino:1550) to confirm the chime attribution. If confirmed and a fix is later wanted, note that resampling /pop.pcm to 16 kHz is NOT sufficient by itself \u2014 the setMonoOutput(true) at Audio.cpp:896 (boot is stereo, Audio.cpp:66 + WiPhone.ino:954) still forces both reinstalls via the mono term of the cache check at Audio.cpp:131-136 and restore()'s monoChanged at Audio.cpp:865; a real fix must also avoid the mono flip, which is a larger, separately-reviewed change.",
   "reasoning": "Claim verified in code: boot installs I2S at 16000 Hz stereo (Audio.cpp:65-66; constructed stereoOut=true at WiPhone.ino:954). playPop (Audio.cpp:889-917) calls setSampleRate(8000) at :895 \u2014 which changes rate in-place via i2s_set_sample_rates (:1409) WITHOUT updating the i2sRate cache \u2014 then setMonoOutput(true) at :896 whose configureI2S misses the cache (:131-136) on both rate and mono, doing uninstall+reinstall #1 of ~16 KB internal DMA RAM (:115-121, previously measured on hardware as 15,648\u21928,200 largest-block drop for the same class of event). restore() (:859-887) then sees needI2S (rate and mono both differ) and does reinstall #2. So two churn cycles per chime \u2014 every chime, not only the first \u2014 interleaved with SIP/mirror allocations: a credible fragmentation-step source, correctly labeled a hypothesis. CORRECTION to the finding's primary fix: resampling /pop.pcm to 16 kHz alone would NOT make the cache hit, because the stereo\u2192mono flip (boot monoOut=false vs playPop's setMonoOutput(true)) independently forces the reinstall, and restore() reinstalls again on monoChanged. Fully eliminating the churn means keeping stereo output for the pop, which drags in codec.setAudioPath routing (:1429) \u2014 beyond a safe blind change on a field device. The finding's zero-risk fallback IS safe and decisive: 'NOTIFY: buzz' is log_e-printed at WiPhone.ino:1536 immediately before the playPop call at :1550, so correlating DROP timestamps against those lines confirms or refutes the attribution with no code change. Confirmed on that basis: mechanism real, safe diagnostic step exists; the resample-only variant should not be shipped as the fix."
  },
  {
   "device": "wiphone",
   "value": "high",
   "risk": "low",
   "effort": "small",
   "lens": "wp-bugs",
   "title": "setRead writes partition unread count into the global counter",
   "file": "/Users/nickhowe/wiphone/WiPhone/Storage.cpp",
   "line": 1258,
   "claim": "Messages::setRead computes `int globalUnread = index[0].getIntValueSafe(\"u\", unread+1) - 1;` (line 1256) and branches on it, but the true branch writes the wrong variable: `index[0][\"u\"] = unread;` \u2014 the PARTITION's remaining unread, not globalUnread. hasUnread() (Storage.h:270) reads exactly index[0][\"u\"], and it drives state.unreadMessages and the status-bar badge (GUI.cpp:164, 6778, 12070). With one inbox partition the two numbers coincide, so it survives testing; once the inbox exceeds PARTITION_SIZE=100 (Storage.h:335) \u2014 which SMS mirroring makes realistic \u2014 reading the last unread in one partition while another partition still holds unread writes 0 into the global counter and the badge wrongly clears (or writes a too-small number). load() never recomputes \"u\", so the corruption persists on disk across reboots. Fix is one token: write globalUnread. Same wrong-variable class as the getAckTime \"t\"/\"a\" bug.",
   "change": "/Users/nickhowe/wiphone/WiPhone/Storage.cpp:1258 \u2014 change `index[0][\"u\"] = unread;` to `index[0][\"u\"] = globalUnread;`",
   "reasoning": "Verified adversarially. (a) Claim is factually right: Storage.cpp:1256 computes globalUnread from index[0][\"u\"], but line 1258 writes `index[0][\"u\"] = unread;` \u2014 the partition's remaining unread count \u2014 leaving globalUnread computed but unused in the true branch. The counters are genuinely distinct: saveMessage increments the per-partition counter at line 936 and the global counter separately at line 974 (\"for entire database\"). hasUnread() (Storage.h:270) reads exactly index[0][\"u\"] and drives state.unreadMessages / the status-bar badge (GUI.cpp:164, 6778, 7396, 12070). Concrete failure: with unread in two partitions, reading the last unread of one partition yields unread=0 while globalUnread>0, so the code writes 0 into the global counter and the badge wrongly clears; the corrupted value is persisted by index.store() (line 1265) and Messages::load never recomputes \"u\" (grep confirms no \"u\" access in load), so it survives reboot. Multi-partition inboxes are realistic given PARTITION_SIZE=100 (Storage.h:335) and SMS mirroring. (b) The change is safe: in the single-partition case unread == globalUnread (both incremented in lockstep at 936/974), so the fix is behavior-identical to today on every configuration that currently works, and correct once a second partition exists. One-token, no other readers or writers affected."
  },
  {
   "device": "wiphone",
   "value": "high",
   "risk": "med",
   "effort": "medium",
   "lens": "wp-bugs",
   "title": "findMessage shallow-copies IniFile part2 into part1: aliasing, double free",
   "file": "/Users/nickhowe/wiphone/WiPhone/Storage.cpp",
   "line": 1097,
   "claim": "Messages::findMessage takes `IniFile& part` (bound to part1 by all three callers: deleteMessage:1141, setRead:1228, ingestMirrored:1056) and does `part = part1; ... part = part2;` intending 'point at the cached partition'. But IniFile/Config/LinearArray define NO copy assignment (grep confirms; LinearArray.h:127-131 owns raw `T* arrayDyn`, Config owns `LinearArray<Section*>` whose Sections are deleted in ~Config, NanoINI.cpp:544-548; IniFile owns `char* filenameDyn`, freed in setFilename, Storage.cpp:1445-1450), so `part = part2` is a compiler-generated SHALLOW memberwise copy: part1's old sections leak, and part1 and part2 now share the same Section* array and the same filenameDyn. The very next preload() that reloads part1 (Storage.cpp:741) calls IniFile::load -> setFilename -> unload -> clear(), deleting the shared Sections and freeing the shared filename, then immediately calls part2.unload() (line 743) which deletes the SAME Section pointers again \u2014 double free, heap corruption, reset. Reachable whenever setRead/deleteMessage targets a message whose partition is cached in part2, i.e. whenever a paged scan window spans a partition boundary: PARTITION_SIZE=100 < SIP_THREADS_SCAN=120 (sip_threads.h:61), so any inbox over 100 messages puts MessagesApp::markThreadRead on this path. This is a plausible residual cause of the 'reboots while opening conversations' symptom class that fault 4 only partly explained. Fix: make findMessage select via IniFile* instead of assignment (3 call sites); host-testable. Risk med only because it touches the message store's hot path.",
   "change": "Change Messages::findMessage (Storage.cpp:1081-1113) to select the cached partition via a pointer instead of object assignment: signature becomes `bool findMessage(MessageData& msg, IniFile*& part, int32_t& section)` (or return IniFile*), with `part = &part1; ... part = &part2; ... part = &part1;` at Storage.cpp:1095/1097/1100 and `part->...` in the body. Update the 3 call sites to pass a pointer and dereference: Storage.cpp:1056-1058 (ingestMirrored adopt path), 1141-1143 (deleteMessage), 1228-1230 (setRead). No other behavior change; verify with the existing host test suites before flashing.",
   "reasoning": "Verified every link in the chain by reading the code. (1) All three callers bind the ref parameter to part1: Storage.cpp:1056, 1141, 1228 all read `IniFile& ini = part1;`. Since C++ references cannot be reseated, `part = part2` at Storage.cpp:1097 invokes assignment on part1 itself. (2) No user-defined copy assignment exists anywhere in the chain: IniFile (Storage.h:125-151) owns raw `char* filenameDyn` (freed in setFilename, Storage.cpp:1445-1451); NanoIni::Config (NanoINI.h:159-301) owns `LinearArray<Section*> sections` whose Section objects are deleted in ~Config/clear (NanoINI.cpp:544-559); LinearArray (LinearArray.h:127-131) owns raw `T* arrayDyn`, freed in clear() (LinearArray.h:366-370). The compiler-generated memberwise copy therefore makes part1 and part2 share the same Section* array buffer and the same filenameDyn, leaking part1's old contents. (3) The double free is concrete: preload (Storage.cpp:740-743) calls loadPartition(part1,...) -> IniFile::load -> setFilename -> unload -> Config::clear(), deleting the shared Sections and freeing the shared arrayDyn/filenameDyn, then immediately calls part2.unload() (line 743), which iterates the freed buffer (stale arraySize) and deletes the same Section pointers again. Messages teardown (Storage.cpp:629-633) unloads both parts and double-deletes too. (4) Reachable in practice: part2 is populated whenever a preload window spans a partition boundary (Storage.cpp:763-764), and PARTITION_SIZE=100 (Storage.h:335) < SIP_THREADS_SCAN=120 (sip_threads.h:61), so any inbox over 100 messages loads part2 during thread scans; setRead/deleteMessage on a message cached in part2 then takes the 1097 branch. The fix (select partition via pointer instead of assignment) is mechanical, behavior-preserving on the healthy path, host-testable before flashing, and confined to findMessage plus 3 call sites \u2014 safe within the med-risk gate."
  },
  {
   "device": "wiphone",
   "value": "med",
   "risk": "low",
   "effort": "small",
   "lens": "wp-bugs",
   "title": "SMS-mirror stall timeout is dead: deadline re-arms on every empty pass",
   "file": "/Users/nickhowe/wiphone/WiPhone/sms_mirror_poll.cpp",
   "line": 418,
   "claim": "The read loop is `while (budget-- > 0 && s_client.available())` (line 382); `budget--` evaluates before available(), so even a pass that reads ZERO bytes leaves budget at 511. The re-arm check `if (budget < READ_BUDGET) s_deadlineMs = now + READ_TIMEOUT_MS;` (lines 418-420) is therefore true on EVERY main-loop pass, bytes or not, so the 8 s stall deadline (checked at line 371) can never expire while connected. The comment block at lines 22-29 ('8 s of genuine silence still kills the connection') is contradicted by the code: a wedged-but-connected COVEY is only cut by TOTAL_BUDGET_MS at 120 s (line 375), holding the socket and the 'reading' status for 2 minutes per attempt. Fix: count bytes actually consumed and re-arm only when nonzero \u2014 small, and the module is host-testable. This is a bug inside the recent 'stall-based poll timeout' work, not a re-report of it.",
   "change": "/Users/nickhowe/wiphone/WiPhone/sms_mirror_poll.cpp:380-420 \u2014 add a local byte counter (e.g. `int consumed = 0;` at line 380, `++consumed;` after the successful `s_client.read()` at line 383), and change line 418 from `if (budget < READ_BUDGET)` to `if (consumed > 0)` so `s_deadlineMs = now + READ_TIMEOUT_MS;` re-arms only when bytes actually arrived. Update the line-419 comment to match. Verify with a host-side test: connected socket, zero bytes for >8 s \u2192 finish(\"timed out\"); bytes trickling slower than 8 s gaps \u2192 also times out; steady slow progress \u2192 stays alive until idle/close.",
   "reasoning": "Verified in /Users/nickhowe/wiphone/WiPhone/sms_mirror_poll.cpp. Line 382's loop condition `budget-- > 0 && s_client.available()` decrements budget before checking available(), so budget is always <= 511 after the loop even on a zero-byte pass; the re-arm guard at line 418 (`budget < READ_BUDGET`) is therefore unconditionally true, pushing s_deadlineMs forward 8 s on every main-loop pass. The stall check at line 371 can never expire while connected; a wedged-but-connected COVEY is only cut by TOTAL_BUDGET_MS (120 s, line 375). This directly contradicts the stated design at lines 22-29 (\"8 s of genuine silence still kills the connection\"). The fix \u2014 re-arm only when bytes were actually consumed \u2014 is small, self-contained, restores documented intent, cannot resurrect the slow-catch-up regression (any pass that reads bytes still re-arms), and only tightens an existing failure path (timeout at 8 s of silence instead of 120 s). Safe on a working device."
  },
  {
   "device": "wiphone",
   "value": "med",
   "risk": "low",
   "effort": "small",
   "lens": "wp-bugs",
   "title": "ViewMessageApp is dead code; message Delete is now unreachable",
   "file": "/Users/nickhowe/wiphone/WiPhone/GUI.cpp",
   "line": 7370,
   "claim": "`new ViewMessageApp` appears nowhere in the tree (only its own constructor at GUI.cpp:7370), and GUI_APP_VIEW_MESSAGE exists only in its declaration (GUI.h:400, 1630) \u2014 the CHATS/THREAD rewrite of MessagesApp stopped creating it but left the whole class compiled in (GUI.cpp:7370-7508, GUI.h:1625-1650, ~2-3 KB flash plus its icon/text plumbing). The stale comment at GUI.h:1610 still says subApp 'can be CreateMessageApp or ViewMessageApp'. Knock-on fact worth surfacing to the owner: ViewMessageApp's Options>Delete (GUI.cpp:7468) was the ONLY caller of Messages::deleteMessage (Storage.cpp:1119/1135), so deleting a message is now impossible from the UI \u2014 dead store-management code carried for a feature that silently vanished. Removing the class (or re-wiring delete into the thread view) is mechanical; pure removal is the low-risk option.",
   "change": "Remove the ViewMessageApp class: declaration at GUI.h:1625-1650 and implementation block at GUI.cpp:7368-7508 (constructor 7370, destructor 7403, processEvent 7415, changeState 7479, redrawScreen 7498). Trim the stale comment at GUI.h:1610 to \"// can be CreateMessageApp\". Lowest-risk variant: leave the GUI_APP_VIEW_MESSAGE enum member at GUI.h:400 untouched (its removal is also safe \u2014 no numeric ID persistence \u2014 but keeping it makes the diff purely subtractive of unreachable code). Leave Storage.cpp deleteMessage in place (now uncalled but harmless). Verify by full firmware compile. Separately surface to the owner, as a decision not a change: message Delete is now UI-unreachable; re-adding it to the THREAD view would be a new feature, out of scope for a low-risk pass.",
   "reasoning": "Every factual claim checks out against the code. (1) ViewMessageApp is never instantiated: grep finds no `new ViewMessageApp` anywhere; the only sub-app creations are `new CreateMessageApp` at GUI.cpp:6837/6862/7463 (7463 is inside ViewMessageApp itself). (2) GUI_APP_VIEW_MESSAGE appears only at GUI.h:400 (enum member) and GUI.h:1630 (ViewMessageApp's own getId); the compiled-in menu table (GUI.h:2703) and enterApp switch (GUI.cpp:1524) reference GUI_APP_MESSAGES symbolically, and no numeric app-ID is persisted (GUI.cpp:1246 \"APP:%d\" is a debug-fallback drawString only). (3) Messages::deleteMessage (Storage.cpp:1119/1135) has exactly one external caller, GUI.cpp:7468, inside dead ViewMessageApp::processEvent \u2014 the rewritten MessagesApp (CHATS/THREAD/COMPOSING, GUI.h:1591-1595) has no Delete option, so UI deletion is genuinely unreachable. (4) The stale comment at GUI.h:1610 is verbatim as claimed; ViewMessageApp's internal copy at GUI.h:1646 was already corrected to \"can be CreateMessageApp (in responseMode)\", corroborating the class was orphaned by the rewrite, not kept intentionally. Safety: deleting a never-instantiated class is compile-time-verified and cannot alter any runtime path on the deployed device; the low-risk variant is pure removal (not re-wiring delete, which is a feature change). Leaving the GUI_APP_VIEW_MESSAGE enum member in place makes the change strictly subtractive of unreachable code."
  },
  {
   "device": "wiphone",
   "value": "med",
   "risk": "low",
   "effort": "small",
   "lens": "wp-bugs",
   "title": "Thread rebuild after composer exit is unreachable; Reply exits to CHATS",
   "file": "/Users/nickhowe/wiphone/WiPhone/GUI.cpp",
   "line": 6813,
   "claim": "In MessagesApp::processEvent's subApp-exit branch, `if (appState == THREAD) this->buildThread();` (6813-6815) can never fire: both CreateMessageApp spawn sites set appState = COMPOSING first (6836-6837 from CHATS, 6861-6862 from THREAD Reply), so appState is always COMPOSING while subApp exists, and `enterState(appState == COMPOSING ? CHATS : appState)` (6816) always lands in CHATS. The code plainly intends 'return to the thread and show the reply you just sent', but replying from an open thread instead dumps you to the chats list, and the intended thread rebuild is dead logic. Fix mirrors MeshtasticApp's existing returnState pattern (app_meshtastic.cpp:456): remember the pre-compose state and re-enter it. Small, contained to this one method.",
   "change": "/Users/nickhowe/wiphone/WiPhone/GUI.cpp \u2014 add a `MessagesState_t returnState` member to MessagesApp (GUI.h class decl); at GUI.cpp:6836 set `returnState = CHATS;` and at GUI.cpp:6861 set `returnState = THREAD;` before `appState = COMPOSING;`; in the subApp-exit branch change GUI.cpp:6813 to `if (returnState == THREAD) { this->buildThread(); }` and GUI.cpp:6816 to `enterState(returnState);`. This is the same returnState pattern already proven in MeshtasticApp (app_meshtastic.cpp:456).",
   "reasoning": "Verified adversarially against the source. (a) Claim is factually right: the only two sites that create the MessagesApp subApp are GUI.cpp:6836-6837 (CHATS \"New message\") and GUI.cpp:6861-6863 (THREAD \"Reply\"), and both set `appState = COMPOSING` immediately before `subApp = new CreateMessageApp(...)`. No other code path makes subApp non-NULL (GUI.cpp:7046 is CreateMessageApp's own unrelated `subApp = NULL`), and the only other appState writes are enterState (GUI.cpp:6726) and the NEW_MESSAGE_EVENT branch (6795-6803), which never changes appState. So while subApp exists appState is always COMPOSING, meaning `if (appState == THREAD) this->buildThread();` at GUI.cpp:6813-6815 can never fire, and `enterState(appState == COMPOSING ? CHATS : appState)` at 6816 always lands in CHATS \u2014 replying from an open thread dumps the user to the chats list, and the thread-rebuild code the author wrote for the return path is dead. (b) The implied change is safe on a working device: threadPeer/threadUri are member char arrays that persist untouched while the composer is open (only openThread at 6782-6788 rewrites them), buildThread() is already exercised on this exact data from two live call sites (6786 on open, 6801 on NEW_MESSAGE_EVENT with the thread open), and enterState(THREAD) at 6721-6724 only sets title/footer from threadPeer. The fix mirrors an existing, proven pattern in the same codebase (MeshtasticApp's returnState, app_meshtastic.cpp:456-461), touches only MessagesApp::processEvent plus one member, and the cancel-composer path returns to the thread just as harmlessly as the sent path. Low risk, small effort, real UX value."
  },
  {
   "device": "wiphone",
   "value": "high",
   "risk": "low",
   "effort": "small",
   "lens": "wp-ux",
   "title": "Redial: dialer always opens empty, last dialed number is kept nowhere",
   "file": "/Users/nickhowe/wiphone/WiPhone/GUI.cpp",
   "line": 2579,
   "claim": "DialingApp::processEvent (GUI.cpp:2566-2605) places a call with textArea->getText() and nothing ever records the number: grep for lastDialed/redial finds no hits anywhere in the firmware. Pressing CALL from the clock just opens the menu (ClockApp:6546 treats CALL via LOGIC_BUTTON_OK as exit), so re-calling the last number means re-typing it. Small fix: store the dialed string into a new ControlState char field at the moment the call starts (GUI.cpp:2584, and PhonebookApp::becomeCaller GUI.cpp:4250 for completeness), and in DialingApp when LOGIC_BUTTON_OK arrives with an empty textArea, prefill from that field instead of calling an empty URI (today it proceeds to 'CALLING ' with an empty string). RAM-only, additive, no persistence needed.",
   "change": "Add a small last-dialed field to ControlState (GUI.h:~246, e.g. char* lastDialedDyn or fixed char[71] buffer, mirroring calleeNameDyn/calleeUriDyn). Write it at call start in DialingApp::processEvent (GUI.cpp:2584, copy textArea->getText()) and in PhonebookApp::becomeCaller (GUI.cpp:4250, copy the phonebook \"s\" URI). In DialingApp's LOGIC_BUTTON_OK branch (GUI.cpp:2579), if textArea is empty and the field is non-empty, prefill textArea from it and return REDRAW_SCREEN instead of placing an empty-URI call. RAM-only; no persistence.",
   "reasoning": "All factual claims verified by reading the code. (1) DialingApp::processEvent (GUI.cpp:2579-2593) calls setRemoteNameUri with textArea->getText() and never records the number; grep across the firmware for lastDial/redial/last_dialed/lastCalled finds no redial mechanism (only tinySIP's unrelated restoreDialogContext). (2) ClockApp:6546 treats LOGIC_BUTTON_OK (CALL) as EXIT_APP, so CALL from the clock does not redial. (3) There is no empty-text guard in the dialer OK path: an empty textArea today proceeds to setRemoteNameUri(\"Dialed number\",\"\") and constructs CallApp \u2014 the prefill would replace an already-broken action, not working behavior. (4) The proposed storage sites are correct: GUI.cpp:2584 (dialer) and PhonebookApp::becomeCaller GUI.cpp:4250 (phonebook), and ControlState already holds analogous strdup'd fields (calleeNameDyn/calleeUriDyn, GUI.h:245-246). Safety: additive, RAM-only, touches only the empty-textArea path; normal dial, phonebook, and incoming-call paths unchanged; heap cost one short string on a 20-30 KB heap. A dedicated field is correctly preferred over reusing calleeUriDyn, which incoming calls overwrite."
  },
  {
   "device": "wiphone",
   "value": "high",
   "risk": "low",
   "effort": "medium",
   "lens": "wp-ux",
   "title": "Missed calls vanish without a trace; call log is a 5-year-old TODO with icons already shipped",
   "file": "/Users/nickhowe/wiphone/WiPhone/GUI.h",
   "line": 2701,
   "claim": "GUI.h:2701 reads '// TODO: call log (icons: Call_log_b/Call_log_w)' and grep confirms no missed/call-log code exists anywhere. becomeCallee (GUI.cpp:1433) creates the CallApp for an incoming call; if it rings out or is declined, CallApp exits (CALL_UPDATE_EVENT -> Idle -> EXIT_APP at GUI.cpp:5057-5060) and nothing is recorded \u2014 the clock, header, and menus show nothing, ever. Minimal safe version: a RAM-only ring of {name, uri, time, missed?} appended in becomeCallee/becomeCaller and marked missed when the call ends without ever reaching CallState::Call, plus a 'Missed call: <peer>' line on ClockApp::redrawScreen (GUI.cpp:6607 area, below the date) cleared when the dialer or phonebook is opened. A full Call-log menu app can come later; the clock line alone removes the silent-loss failure mode. Additive only; no existing paths change.",
   "change": "Add a small static call-record ring (e.g. 8 entries of fixed char[24] name / char[48] uri / uint32 time / bool missed, no heap allocation): append an entry in GUI::becomeCallee (GUI.cpp:1433) and PhonebookApp::becomeCaller (GUI.cpp:4246); in CallApp, set a reachedCall flag when sipState hits CallState::Call (GUI.cpp:~5061) and mark the entry missed in the Idle/EXIT_APP path (GUI.cpp:~5058) if the flag never set for a callee-side call; draw one \"Missed call: <name>\" line in ClockApp::redrawScreen below the date (GUI.cpp:~6600), cleared when the dialer or phonebook app is opened.",
   "reasoning": "Every cited anchor checks out: GUI.h:2701 holds the TODO in the main-menu table, icons already shipped (src/assets/icons.h:969/1000, plus unused icon_missed_w at :76), grep confirms zero call-log/missed-call code in the firmware, becomeCallee (GUI.cpp:1433) only constructs CallApp, and CallApp exits on Idle via EXIT_APP (GUI.cpp:~5058) recording nothing \u2014 so a declined or rung-out call truly vanishes. ClockApp::redrawScreen (GUI.cpp:6566, date at ~6598) has room for the proposed one-line notice, and the peer identity strings needed for the record already exist in ControlState (GUI.cpp:343-349, 488). The minimal change is additive (RAM ring + flag + one drawString), alters no existing SIP or lifecycle logic, and is safe on a deployed device with one constraint: given the documented internal-heap fragmentation panic (comment at GUI.cpp:5065+) and that new/malloc is always internal in this build, the ring must use fixed static char buffers, never strdup/new. Effort is honestly medium, value high, risk low with that constraint."
  },
  {
   "device": "wiphone",
   "value": "med",
   "risk": "low",
   "effort": "small",
   "lens": "wp-ux",
   "title": "Reboot is a one-press top-level menu item that calls ESP.restart() with no confirmation",
   "file": "/Users/nickhowe/wiphone/WiPhone/GUI.cpp",
   "line": 1073,
   "claim": "Menu row {13, 1, \"Reboot\", ..., GUI_ACTION_RESTART} (GUI.h:2710) sits in the main menu, and the handler at GUI.cpp:1073-1074 executes ESP.restart() immediately on OK \u2014 one mis-press (it is adjacent to Settings) instantly reboots, losing all RAM state (e.g. any parked mesh/session state). Cheapest robust fix uses machinery that already exists: change row 13's action to GUI_ACTION_SUBMENU and add one child row {45, 13, \"Yes, reboot now\", ..., GUI_ACTION_RESTART} \u2014 enterMenu/exitMenu handle the rest generically (GUI.cpp:1066-1075, 1353-1426). Must bump menu[41] to menu[42] per the size warning at GUI.h:2687-2692 (too-small is silent zero-fill; the comment documents this trap and enterMenu already skips title-less rows).",
   "change": "GUI.h:2710 \u2014 change row 13's action from GUI_ACTION_RESTART to GUI_ACTION_SUBMENU (and preferably set its buttons to \"Select\", \"Back\" like the other submenus); add child row { 45, 13, \"Yes, reboot now\", \"\", \"\", GUI_ACTION_RESTART } in the same table; GUI.h:2692 \u2014 bump menu[41] to menu[42]. No GUI.cpp changes needed.",
   "reasoning": "Claim verified by reading the code: GUI.h:2710 defines Reboot as a top-level main-menu row {13, 1, \"Reboot\", \"\", \"\", GUI_ACTION_RESTART}, adjacent to Settings (GUI.h:2709), and GUI.cpp:1073-1074 calls ESP.restart() immediately on OK with no confirmation \u2014 one mis-press reboots and loses all RAM state. The fix is safe: GUI_ACTION_SUBMENU is handled fully generically by enterMenu() (GUI.cpp:1353-1414, tolerates icon-less children at 1383-1384) and exitMenu()/Back (GUI.cpp:1416-1426, 1038-1043) correctly return to the main menu; ID 45 is free (GUI.h:2758 comment confirms 0-44 taken, 44 used at 2762) and the boot-time duplicate-ID check (GUI.cpp:229-248) guards mistakes; the required menu[41]\u2192menu[42] bump is the documented trap at GUI.h:2687-2692 and the finding accounts for it. The change uses only existing, repeatedly-exercised machinery (five submenus already work this way), touches two files, and is purely additive."
  },
  {
   "device": "wiphone",
   "value": "med",
   "risk": "low",
   "effort": "small",
   "lens": "wp-ux",
   "title": "Phonebook 'Delete' erases and persists the contact on a single OK with no confirmation",
   "file": "/Users/nickhowe/wiphone/WiPhone/GUI.cpp",
   "line": 4130,
   "claim": "In PhonebookApp OPTIONS state, selecting Delete (sel==0x102, GUI.cpp:4130-4136) immediately does flash.phonebook.removeSection(currentKey) + store() \u2014 the contact is gone from flash with no confirm and no undo. The options menu is one OK away from VIEWING, and Delete sits directly under Edit (0x101), so a double-press or off-by-one is destructive. Fix in the same pattern as the OPTIONS state itself: a CONFIRM_DELETE state whose OptionsMenuWidget offers 'Delete contact' / 'Cancel', defaulting to Cancel. Purely additive state in one app.",
   "change": "In /Users/nickhowe/wiphone/WiPhone/GUI.h:2347 append CONFIRM_DELETE to PhonebookAppState_t and declare an OptionsMenuWidget* confirmDelete member. In /Users/nickhowe/wiphone/WiPhone/GUI.cpp: (1) at 4130-4136 replace the immediate removeSection/store with res |= changeState(CONFIRM_DELETE); (2) add a changeState branch (near 3908-3928, before the final else at 3934) that lazily builds a two-option menu \u2014 \"Cancel\" (0x201, listed first so it is the default) and \"Delete contact\" (0x202) \u2014 sets focus, and titles the header \"Delete contact?\"; (3) add a processEvent branch for CONFIRM_DELETE where 0x202 performs removeSection(currentKey)+store()+createLoadMenu()+changeState(SELECTING), while 0x201 and LOGIC_BUTTON_BACK return to VIEWING; (4) delete the new widget in the destructor next to GUI.cpp:3857-3860.",
   "reasoning": "Claim verified by reading the code. GUI.cpp:4130-4136: in the OPTIONS state, sel==0x102 (\"Delete\") immediately calls flash.phonebook.removeSection(currentKey) and, on success, flash.phonebook.store() \u2014 the contact is erased and persisted to flash with no confirmation and no undo path. The menu built at GUI.cpp:3917-3920 orders options Edit(0x101), Delete(0x102), Call(0x103), Send message(0x104), so Delete is directly under Edit, and OPTIONS is one SELECT press from VIEWING (GUI.cpp:4156-4159), making an accidental destructive press plausible. The PhonebookAppState_t enum (GUI.h:2341-2348) has no confirm state today. The implied fix is genuinely small and self-contained: append one enum value, add one changeState branch (copying the lazy OptionsMenuWidget pattern at GUI.cpp:3914-3922, with Cancel as the first/default option), add one processEvent branch, redirect the 0x102 case to the new state, and delete the new widget in the destructor (pattern at GUI.cpp:3857-3860). UI-only, touches one app, no flash-format/radio/timing implications, bench-testable \u2014 safe on a working device. One caution for the implementer: changeState's trailing else (GUI.cpp:3934) treats any unhandled state as ADDING/VIEWING/EDITING, so the new state must get its own explicit branch."
  },
  {
   "device": "wiphone",
   "value": "med",
   "risk": "low",
   "effort": "small",
   "lens": "wp-ux",
   "title": "In-call volume keys read uninitialized volumes when the INI is missing, and write flash on every keypress",
   "file": "/Users/nickhowe/wiphone/WiPhone/GUI.cpp",
   "line": 5082,
   "claim": "CallApp::processEvent UP/DOWN: earpieceVol/headphonesVol/loudspeakerVol are declared uninitialized at GUI.cpp:5082; the audio->getVolumes() call that would seed them is commented out at 5083, and they are only assigned inside 'if (ini.load() && !ini.isEmpty())' + hasSection(\"audio\") (5084-5090). If the INI is absent/corrupt, +=6 at 5100-5103 operates on stack garbage, and that garbage is then STORED to the INI (5114-5117) and pushed to the codec (5121) \u2014 one volume press can persist junk volumes. Separately, ini.load()+ini.store() runs on every single volume keypress during a live call (flash write + parse in the audio-critical loop). Fix: uncomment/seed from audio->getVolumes() as fallback, clamp, and store once (e.g. on call end or first idle) instead of per press. Behavior-preserving for the normal path where the INI exists.",
   "change": "Minimal safe core: (1) uncomment GUI.cpp:5083 so the locals are seeded via audio->getVolumes(earpieceVol, headphonesVol, loudspeakerVol) before the INI overlay \u2014 identical to the pattern at WiPhone.ino:967 and GUI.cpp:2960. (2) Move the three ini[\"audio\"][...] assignments and ini.store()/ini.unload() (GUI.cpp:5114-5120) to after the setVolumes/getVolumes readback at 5121-5122 so the persisted values are the clamped ones, fixing the unbounded-growth/int8 wrap on held keys. Optional (slightly larger, defer if cautious): store once on the EXIT_APP path (GUI.cpp:5060) instead of per keypress to remove the flash write from the in-call loop.",
   "reasoning": "Claim verified by reading the code. GUI.cpp:5082 declares the three int8_t volumes uninitialized; the seeding audio->getVolumes() at 5083 is commented out; assignment happens only under ini.load() + hasSection(\"audio\") (5084-5090), so a missing/corrupt INI (or even one missing key, since the uninitialized value is passed as the getIntValueSafe default at 5087-5089) leaves stack garbage that gets +=6 (5100-5103), persisted to the INI (5114-5117), and sent to the codec (5121). The codec path is bounded because Audio::setVolumes clamps (Audio.cpp:337-354), but the INI stores the pre-clamp value \u2014 which also means repeated presses grow the stored int8_t without bound and wrap at 127 even with a healthy INI. The fix is safe: seeding from audio->getVolumes() is the codebase's own established pattern at both WiPhone.ino:966-967 (boot) and GUI.cpp:2959-2960 (volume settings app), and Audio's members have sane initializers (Audio.h:290, earpieceVol = 6), so behavior is unchanged whenever the INI has all three keys. The per-keypress ini.load()+ini.store() flash write is also confirmed (5084/5117/5120 per WIPHONE_KEY_UP/DOWN); deferring the store to call end is the only part of the suggestion that is more than trivial, so it should be treated as optional relative to the two-line correctness fix."
  },
  {
   "device": "wiphone",
   "value": "med",
   "risk": "low",
   "effort": "small",
   "lens": "wp-ux",
   "title": "No battery percentage anywhere in the normal UI \u2014 icon is 5 coarse levels only",
   "file": "/Users/nickhowe/wiphone/WiPhone/GUI.cpp",
   "line": 11882,
   "claim": "drawBatteryIcon (GUI.cpp:11882-11934) maps controlState.battSoc to one of six icons; no screen prints the number (grep for battSoc shows only the icon logic and diagnostics). controlState.battSoc is already maintained, and ClockApp::redrawScreen draws the icon at GUI.cpp:6581 with empty space to its left. Small fix: on the clock screen only, draw a right-anchored 'NN%' string (AKROBAT_BOLD_16/18, white-on-transparent like the icons) just left of the icon, using the width the icon call already returns. Optionally the same one-liner in HeaderWidget::redraw (GUI.cpp:12147) later; clock-only keeps the change to one function. For a device going on a multi-day hunt, coarse 5-level SOC is a real planning gap.",
   "change": "GUI.cpp:6581 (ClockApp::redrawScreen): capture the icon width \u2014 `auto bw = GUI::drawBatteryIcon(lcd, controlState, -1, lcd.width()-3, 7);` \u2014 then, only if bw>0, draw the number right-anchored just left of the icon: set fonts[AKROBAT_BOLD_16], TR_DATUM, setTextColor(WHITE, BLACK) with setSmoothTransparency(true), snprintf a buffer with \"%.0f%%\" from controlState.battSoc (it is a float, GUI.h:301), and drawString at x = lcd.width()-3-bw-4, y = 7. No other files touched; clock screen only.",
   "reasoning": "Claim verified by reading the code. (a) Factually right: drawBatteryIcon (GUI.cpp:11882-12003) maps battSoc to 6 coarse icons (or 0-5 sections in extended mode) with no numeric output; a full grep for battSoc shows the only percentage text in the firmware is DiagnosticsApp::updateVoltage (GUI.cpp:8112), i.e. diagnostics only \u2014 no normal-UI screen prints the number. ClockApp::redrawScreen calls drawBatteryIcon right-anchored at GUI.cpp:6581, discards the returned width, and the space to the icon's left is empty (other status icons are all left-anchored at 6578-6580). (b) Safe: ClockApp already returns REDRAW_SCREEN on BATTERY_UPDATE_EVENT and BATTERY_BLINK_EVENT (GUI.cpp:6552) so the number stays current with no new plumbing; the screen is fully repainted each redraw (6570-6575) so no stale pixels; drawBatteryIcon returns 0 when battVoltage<=0 (12002), a natural guard against drawing a bogus value before the first gauge read; fonts AKROBAT_BOLD_16/18 exist and are loaded (GUI.h:162-163, GUI.cpp:1115-1116). Display-only, confined to one function, no heap or timing impact on an ESP32 with tight internal heap. Genuinely small and self-contained UX addition with real value for a multi-day off-grid trip. Two implementation precisions: battSoc is a float (GUI.h:301) so use %.0f%%, and set font/text-datum before drawing since redrawScreen only configures them afterward."
  },
  {
   "device": "wiphone",
   "value": "med",
   "risk": "low",
   "effort": "small",
   "lens": "wp-ux",
   "title": "One-press path from clock to Messages when the envelope icon is showing",
   "file": "/Users/nickhowe/wiphone/WiPhone/GUI.cpp",
   "line": 6559,
   "claim": "The clock draws the unread envelope (GUI.cpp:6580) but offers no way to act on it \u2014 reading a new text is OK(menu) + navigate to Messages + OK, and the mesh/SIP thread machinery adds more presses. ClockApp already has the pattern for app shortcuts: digits return EXIT_APP | ENTER_DIAL_APP (GUI.cpp:6559-6561) and GUI.cpp:1004-1009 routes it. Fix: add ENTER_MESSAGES_APP as 0x20 in the appEventResult flag space (GUI.h:150-158 \u2014 0x20/0x40 are free), return it from ClockApp for e.g. the '#' key (or UP) when controlState.unreadMessages || meshUnread, and add one branch beside the ENTER_DIAL_APP branch in GUI::processEvent that calls enterApp(GUI_APP_MESSAGES). Mirrors an existing, proven mechanism.",
   "change": "1) /Users/nickhowe/wiphone/WiPhone/GUI.h:154 \u2014 add `#define ENTER_MESSAGES_APP 0x20` beside ENTER_DIAL_APP (0x20 is free in the uint8_t flag space). 2) /Users/nickhowe/wiphone/WiPhone/GUI.cpp:6557 \u2014 in ClockApp::processEvent, before the digit branch, add: `if (event == '#' && (controlState.unreadMessages || controlState.meshUnread)) return EXIT_APP | ENTER_MESSAGES_APP;` (already below the locked guard). 3) /Users/nickhowe/wiphone/WiPhone/GUI.cpp:1009 \u2014 beside the `else if (appRes & ENTER_DIAL_APP)` branch, add `else if (appRes & ENTER_MESSAGES_APP) { enterApp(GUI_APP_MESSAGES); }` \u2014 without forwarding the event into the new app (unlike the dial branch).",
   "reasoning": "All cited facts verified by reading the code. GUI.cpp:6557-6559 shows ClockApp already returns EXIT_APP|ENTER_DIAL_APP for digit/*/# keys, positioned after the controlState.locked guard so a new branch is automatically lock-gated. GUI.cpp:6580 draws the envelope, driven by controlState.unreadMessages/meshUnread (GUI.h:290-291, GUI.cpp:12070-12071). GUI.h:150-158 confirms appEventResult bits 0x20/0x40 are unused. GUI.cpp:1004-1010 has the exact ENTER_DIAL_APP routing branch to mirror, and GUI.cpp:1523-1525 shows enterApp(GUI_APP_MESSAGES) constructs MessagesApp self-contained (screen, state, flash, header, footer) \u2014 no menu context needed; on exit it returns to the menu via the existing else branch, same as any app. The change is purely additive: no existing path changes when there are no unread messages, and the new behavior is gated on the unread flags. One nuance for the implementer: do NOT forward the triggering event into MessagesApp (the ENTER_DIAL_APP branch forwards so the digit is typed into the dialer; Messages wants no synthetic keypress). Small, self-contained, mirrors a proven mechanism \u2014 safe on a field-deployed device."
  },
  {
   "device": "wiphone",
   "value": "med",
   "risk": "med",
   "effort": "medium",
   "lens": "wp-ux",
   "title": "Theme assessment: colors are compile-time #defines; a boot-time accent/theme picker is feasible, live re-theming is not",
   "file": "/Users/nickhowe/wiphone/WiPhone/GUI.h",
   "line": 108,
   "claim": "All UI colors are preprocessor constants: WP_COLOR_0/1, WP_ACCENT_0/1/S/G, WP_DISAB_0/1 at GUI.h:108-115 and THEME_* at GUI.h:95-102, used ~44 times for the accents alone \u2014 as constructor default arguments and explicit setStyle calls (e.g. GUI.cpp:4017, 6890-6891). Because default args are evaluated at each call site and every app's widgets are constructed fresh in enterApp, converting these defines to plain global colorType variables (initialized to today's values, optionally overridden from an INI in setup before the first app is created) gives a working boot-time theme/accent picker with a ChoiceWidget row in ScreenConfigApp (GUI.cpp:5800), no per-widget plumbing. Hard limits to state honestly: RLE3 icons are fixed-palette and will not recolor (icons.h generates the green envelope by palette rewrite at build time), and a change applies on next app entry, not instantly. Risk is visual-only but broad (every screen), so med \u2014 this is the one to prototype on the bench, not push blind.",
   "change": "Convert the color #defines at /Users/nickhowe/wiphone/WiPhone/GUI.h:98-115 (THEME_* and WP_COLOR_0/1, WP_ACCENT_0/1/S/G, WP_DISAB_0/1) to extern colorType globals declared in GUI.h and defined in GUI.cpp with byte-identical initial values; optionally override them from a [theme] section of Storage::ConfigsFile in setup() before gui.init(); add a ChoiceWidget accent/theme row in ScreenConfigApp (GUI.cpp:5800) that writes the INI and takes effect on next app entry. Prototype on the bench first \u2014 the change is visual-only but touches every screen.",
   "reasoning": "Every cited fact verified by reading the code. Palette defines are at GUI.h:108-115 (WP_COLOR_0/1, WP_ACCENT_0/1/S/G, WP_DISAB_0/1) and THEME_* at GUI.h:98-105; 48 WP_ACCENT usages (10 GUI.h + 38 GUI.cpp) match the ~44 claim. Both cited setStyle sites are real (GUI.cpp:4017, 6890-6891) and ScreenConfigApp is at GUI.cpp:5800 with an existing NanoIni member for persistence. The conversion mechanics hold in all three usage forms: constructor default args (GUI.h:762, 787, 803, 875, 911) evaluate at each call site, in-class member initializers (GUI.h:963-964, 1259-1260) evaluate at construction, and explicit setStyle calls read the value directly \u2014 so plain colorType globals substitute cleanly. Adversarial checks found no landmines: zero static const/constexpr/#if uses of these macros (nothing needs a compile-time constant), and although GUI gui; is a file-scope global (WiPhone.ino:64) constructed before setup(), uint16_t globals with constant initializers are constant-initialized first, and HeaderWidget/FooterWidget store no colors anyway (GUI.h:834-850) \u2014 colors are read at redraw. Safety: behavior-identical when no INI override exists (globals seeded with today's exact values), ~30 bytes RAM, failure domain purely visual; the finding honestly names the limits (RLE3 icons fixed-palette, applies on next app entry) and gates itself to bench prototyping, consistent with its declared med risk which is within the audit's scope."
  },
  {
   "device": "covey",
   "value": "high",
   "risk": "low",
   "effort": "small",
   "lens": "cv-code",
   "title": "SMS mirror LAN server binds once and can be silently dead forever",
   "file": "Project_definition_and_documents/COVEY_RevA_v0.5/firmware/pi/covey-ui/covey_ui/smsmirror.py",
   "line": 316,
   "claim": "Server.start() (smsmirror.py:312-329) binds to lan_ip() captured at start time; lan_ip() (smsmirror.py:76-84) returns '127.0.0.1' when there is no default route yet. If covey-ui.service starts before Wi-Fi gets DHCP (the RAK/boot race in D-027 shows services do start before peripherals settle), the bind to loopback SUCCEEDS and the mirror is unreachable from the phone forever. Worse, App._smsmirror_tick (app.py:666-678) only (re)creates the server when `srv is None`, on pref toggle, or on token change \u2014 so a failed bind (srv.error set, httpd None, active=False) is never retried, and an IP change after roaming networks is never rebound. Nothing anywhere in the UI reads srv.error/srv.served/srv.active (grep: zero references outside smsmirror.py/app.py), so every one of these states is invisible \u2014 the exact silent-mirror failure class already debugged on the WiPhone side. Fix shape: in the tick, if srv exists but not srv.active, or srv.ip != current lan_ip (checked at the existing wifi_up() 3 s cache cadence, not per frame), stop and rebuild; optionally refuse to bind 127.0.0.1 and wait.",
   "change": "In covey_ui/app.py:_smsmirror_tick (the LAN-server block at app.py:669-681), add a health/rebind check gated on the existing 3 s cached IP (reuse the wifi_up()/lan_ip cache pattern from app.py:186-198): if want_lan and srv is not None and (not srv.active or srv.ip != current cached lan_ip), call srv.stop() and set self._smsmirror_srv = None so the next tick rebuilds it. Optionally, in covey_ui/smsmirror.py:Server.start() (~line 316), treat lan_ip() == \"127.0.0.1\" as a failure (set self.error, return False without binding) so the tick keeps retrying until a real LAN address exists.",
   "reasoning": "All factual claims verified by reading the code. smsmirror.py lan_ip() (lines ~76-87) returns \"127.0.0.1\" on OSError (no default route), and Server.start() (lines ~312-329) binds to that IP successfully, leaving active=True and error=\"\" while the phone can never reach it. app.py _smsmirror_tick (lines 669-681) only creates the server when srv is None and only tears it down on pref-off or token change, so a failed bind (OSError leaves srv with httpd=None, error set) is never retried and a LAN IP change is never rebound (srv.ip is never compared to anything). grep confirms only smsmirror.py/prefs.py/app.py reference the mirror \u2014 no screen reads srv.error/srv.served/srv.ip, and there is no tests/test_smsmirror*.py, so every failure state is invisible. App.wifi_up() (app.py:186-198) already provides a 3 s cached lan_ip() cadence to piggyback on. The fix is confined to the tick's lifecycle block plus optionally start(): no radio path, no serving logic, no persisted state touched; the phone holds the since-id so a server rebuild loses nothing. Small, self-contained, and low risk on a working device."
  },
  {
   "device": "covey",
   "value": "med",
   "risk": "low",
   "effort": "small",
   "lens": "cv-code",
   "title": "app.sip_threads is mutated from a worker thread with no lock; two save() writers share one tmp path",
   "file": "Project_definition_and_documents/COVEY_RevA_v0.5/firmware/pi/covey-ui/covey_ui/sipui.py",
   "line": 653,
   "claim": "_echo_locally (sipui.py:646-660) runs on the send worker thread and does threads(app).setdefault(...) + append + siptext.save(). The same dict is concurrently iterated by the UI thread (SipConversationsScreen._rows iterates threads(app).items() at sipui.py:430; drain/merge mutates it at sipui.py:359-362) and by the LAN mirror handler thread (smsmirror.py:278 records(srv.threads_fn()...) walks every thread/message). A setdefault that inserts a new peer mid-iteration raises RuntimeError('dictionary changed size during iteration') \u2014 in a draw path that exception propagates out of the unguarded main loop (main.py:127-139) and kills the UI. Separately, siptext.save() (siptext.py:327-332) always writes STORE+'.tmp': if the send worker and the UI-thread drain save concurrently, both open the same tmp file and interleave json.dump output; os.replace then atomically installs a corrupt file, and load() (siptext.py:316-318) silently returns {} on next boot \u2014 whole SMS history gone with no message. Probability is low (two writes must overlap) but the cost matches the project's worst failure class. Small fix: have the worker only set self._sent and do the echo+save inside update()'s existing _sent drain (sipui.py:579-587), which already runs on the UI thread; the mirror handler can snapshot via list(...) with a try/except.",
   "change": "sipui.py:662-686 \u2014 in run(), stop calling _echo_locally() on the worker thread; instead carry an echo flag in the result tuple (e.g. self._sent = (True, msg, True) on success and on timeout, (False, msg, False) otherwise). sipui.py:579-587 \u2014 in SipThreadScreen.update()'s existing _sent drain (UI thread), when the echo flag is set call _echo_locally() (moved so it is reachable there, still closing over text/peer) before toasting; this puts the setdefault/append/save on the same thread as _rows and drain, eliminating both the dict race and the concurrent-writer .tmp collision from the send path. smsmirror.py:278 \u2014 wrap the handler's read as `try: lines = records(dict(srv.threads_fn() or {}), since_id=since) except RuntimeError: self._deny(503, \"busy\"); return` (snapshot + guard) so mirror threads never raise mid-iteration against UI-thread merges.",
   "reasoning": "Every element of the claim checks out against the code. (1) _echo_locally (sipui.py:646-660) runs on the daemon send-worker thread (started sipui.py:686, called at :665 and :679) and does threads(app).setdefault + append + siptext.save \u2014 unsynchronized dict mutation and a store write off the UI thread. (2) The same dict is iterated concurrently: UI thread in SipConversationsScreen._rows (sipui.py:430, every frame) and mirror handler threads (ThreadingHTTPServer daemon threads, smsmirror.py:318) via records() \u2192 _all_sorted iterating threads.items() (smsmirror.py:278, :217), with threads_fn a live reference to app.sip_threads (app.py:672-674). A setdefault inserting a NEW peer mid-iteration raises RuntimeError('dictionary changed size during iteration'); the concrete window exists (send first message to a new peer from Contacts, back out to the conversations screen during the up-to-45s TIMEOUT_SEND request). (3) main.py:127-140 has no per-frame exception guard (the try at :126 only reaches a cleanup finally), so that RuntimeError kills covey-ui. (4) siptext.save (siptext.py:321-335) always writes STORE+'.tmp'; two concurrent savers (worker echo vs UI-thread drain at sipui.py:367) open/truncate the same path with independent offsets, can interleave json.dump output, and os.replace installs the corrupt file; load() (siptext.py:311-318) silently returns {} \u2014 whole SMS history lost with no message, the project's stated worst failure class. The suggested change is safe: moving the echo+save into update()'s existing _sent drain (sipui.py:579-587) keeps all store mutation on the UI thread, is confined to SipThreadScreen, and changes no wire behavior; the only nuance is that popping the screen before the drain skips the placeholder echo, which is benign because the real record arrives on the next poll and the anti-double-send placeholder only matters while the compose box is visible. The mirror-side list() snapshot under try/except is trivially safe (one failed poll retried in 60 s at worst). Probability of the race is low but nonzero, cost is maximal, fix is small \u2014 a careful engineer would make this change."
  },
  {
   "device": "covey",
   "value": "med",
   "risk": "low",
   "effort": "small",
   "lens": "cv-code",
   "title": "Now Playing volume drag writes prefs.json on every MOUSEMOTION event",
   "file": "Project_definition_and_documents/COVEY_RevA_v0.5/firmware/pi/covey-ui/covey_ui/music.py",
   "line": 663,
   "claim": "NowPlayingScreen.handle's drag path (music.py:639-644) calls _set_vol on every MOUSEMOTION while the finger is down, and _set_vol (music.py:663-667) does app.prefs.save() each time \u2014 a json.dump + os.replace to the SD card at up to 30 Hz on the UI thread for the duration of a drag. The brightness slider already establishes the house pattern for exactly this: set_brightness(save=False) during the drag with a commit on release, documented at app.py:156-160 ('the SD card is not written on every pixel'). The keyboard UP/DOWN path (music.py:635-637) is fine (one save per press). Fix: pass save-on-MOUSEBUTTONUP or debounce, mirroring brightness().",
   "change": "covey_ui/music.py:663 \u2014 change _set_vol signature to `def _set_vol(self, app, v, save=True)` and guard the prefs write: always `p.set_volume(v)` and update `app.prefs[\"music_volume\"]`, but call `app.prefs.save()` only when save is True. At music.py:643 pass `save=(ev.type == pygame.MOUSEBUTTONDOWN)` (or save=False on the MOUSEMOTION drag) and set a `self._vol_dragging = True` flag; in handle(), before the line-645 early return, add: `if ev.type == pygame.MOUSEBUTTONUP and getattr(self, \"_vol_dragging\", False): self._vol_dragging = False; app.prefs.save()`. Commit keys off the drag flag, not rect collision, so a release outside the rect still persists. Keyboard path (music.py:634-637) unchanged.",
   "reasoning": "Claim verified by reading the code: music.py:639-644 calls _set_vol on every MOUSEMOTION with button held over the volume rect, and _set_vol (music.py:663-667) unconditionally calls app.prefs.save(). prefs.py:73-81 shows save() has no debouncing \u2014 each call is makedirs + json.dump to a tmp file + os.replace, i.e. a full SD write on the UI thread at event rate for the duration of a drag. The proposed fix mirrors the documented house pattern at app.py:159-162/172-177 (brightness slider: save=False during drag, commit on release, \"the SD card is not written on every pixel\"), so it is a copy of an existing device-verified idiom. The change is small and self-contained: the keyboard path (music.py:634-637, one save per press) stays untouched via a default save=True, and MOUSEBUTTONUP is currently unhandled (music.py:645 returns early for non-MOUSEBUTTONDOWN), so adding a commit-on-release branch collides with nothing. Audible volume still applies live via p.set_volume; only persistence timing changes. Worst case (power loss mid-drag) loses one slider position \u2014 the same tradeoff brightness already accepted."
  },
  {
   "device": "covey",
   "value": "med",
   "risk": "low",
   "effort": "small",
   "lens": "cv-code",
   "title": "SIP conversations list rebuilds and re-sorts every thread, counting unread over every message, per event and per frame",
   "file": "Project_definition_and_documents/COVEY_RevA_v0.5/firmware/pi/covey-ui/covey_ui/sipui.py",
   "line": 428,
   "claim": "SipConversationsScreen._rows (sipui.py:428-437) walks every message of every thread to compute unread counts, then sorts \u2014 and it is called from handle() (459), _content_h() (445-446, itself called per scroll event via list.handle at 461), and twice per drawn frame (draw 474 and draw_indicator's _content_h at 510). At the store caps (MAX_THREADS=60 x MAX_PER_THREAD=300, siptext.py:68-69) that is ~18k dict reads several times per frame on a Pi 3A+ during a scroll drag \u2014 the exact class of regression just fixed for the thread view (SipThreadScreen._layout's cache, sipui.py:745-779, added after Nick reported 'covey lags heavily... mostly when scrolling'). Today's store is small so it is not yet felt, but the store grows toward the caps by design. Fix: cache rows keyed on (thread count, last_id, read_ids) the same way _layout does.",
   "change": "covey-ui/covey_ui/sipui.py:428 \u2014 add a cache to SipConversationsScreen._rows mirroring SipThreadScreen._layout (sipui.py:745-779): compute key = tuple(sorted((peer, len(msgs), int(msgs[-1].get(\"id\",0)) if msgs else 0, int(t.get(\"read_id\",0))) for peer, t in threads(app).items())); return self._rows_cache when self._rows_key == key, else rebuild and store both. The read_id term is required so marking a thread read clears the badge immediately; the last-id term covers new arrivals and placeholder-id swaps.",
   "reasoning": "Claim verified line-by-line. SipConversationsScreen._rows (covey-ui/covey_ui/sipui.py:428-437) walks every message of every thread to sum unread counts, then sorts; it is invoked from handle() at line 459, again via _content_h(app) at 461 (which runs on every event list.handle sees, i.e. each motion event of a drag), from _open at 453, and twice per drawn frame (draw at 474, draw_indicator's _content_h at 510). Store caps MAX_PER_THREAD=300 and MAX_THREADS=60 confirmed at siptext.py:68-69, so worst case is ~18k dict reads several times per frame during a drag on a Pi 3A+. This is the same regression class already fixed in the same file: SipThreadScreen._layout (sipui.py:745-779) is a cache whose docstring states it was the fix for Nick's reported scroll lag, keyed on message count + boundary ids. The proposed fix copies a proven in-file pattern, is local to one method, O(threads)<=60 key construction per frame, and is exercised by the existing green test suites; worst failure mode of a wrong key is a briefly stale row, not a crash. Not felt today (small store) but the store grows toward the caps by design, so med value / low risk / small effort is the right grading \u2014 a careful engineer would make this change."
  },
  {
   "device": "covey",
   "value": "high",
   "risk": "low",
   "effort": "small",
   "lens": "cv-ux",
   "title": "Parked book-sync position arrives with zero UI signal",
   "file": "Project_definition_and_documents/COVEY_RevA_v0.5/firmware/pi/covey-ui/covey_ui/books.py",
   "line": 881,
   "claim": "poll_mesh_background (books.py:881-900) parks a LoRa position on app._book_pending silently: no toast, no badge, nothing. BooksScreen.draw (books.py:180-218) never consults _book_pending and the Home Books tile badge is hardcoded 0 (screens.py:170). This is the documented trap that made working sync look dead ('the covey got nothing'). Fix is small and draw-only: app.toast(\"Book position received - open <title>\") when parking (guard on not app._locked; app.toast exists at app.py:334), plus an accent dot on BooksScreen rows whose fingerprint matches a pending rec (entry fingerprint derivable without opening the epub, same as _progress at books.py:144-147).",
   "change": "In poll_mesh_background (covey_ui/books.py:899, immediately after pend.append(p)): if not getattr(app, \"_locked\", False): try to resolve a title by scanning BOOKS_DIR entries whose \"fp:\"+epub._fingerprint(path) or \"ta:\"+epub._slug(title+\"|\") intersects p.rec.get(\"ids\"); toast app.toast(\"Book position from %s \u2014 open %s\" % (p.device, title)) when resolved, else app.toast(\"Book position received \u2014 open the book to apply it\"). Optionally (still draw-only) in BooksScreen.draw (books.py:204 area): compute the same two derivable ids per row and draw a small T.ACCENT dot on rows matching any app._book_pending rec \u2014 accepting that records whose sole mesh id is id:<identifier> will show no dot. Do not touch parking, matching, or _pending_pop logic.",
   "reasoning": "Verified every cited fact by reading the code. (a) Claim is right: poll_mesh_background (books.py:881-900) parks mesh sync positions on app._book_pending with no UI signal; BooksScreen.draw (books.py:180-218) never reads _book_pending; the Home Books tile badge is hardcoded 0 (screens.py:170) while the Messages tile proves the badge plumbing exists (screens.py:165). This is the documented trap that made working sync look dead. (b) Change is safe: app.toast exists at app.py:337 (finding said 334 \u2014 trivial drift) and is pure main-loop draw state rendered at app.py:912; poll_mesh_background is already called from App.update() on the main thread (app.py:596-597), so a toast there uses the identical context as every existing toast; app._locked (app.py:49) is available for the guard. No radio, store, or sync-protocol code is touched \u2014 worst case is an unseen toast. One scope correction: the mesh packet carries only ids[0] (booksync.py reduce_for_mesh:143-147), which is the id:/ta:-slug when the sender's epub has an identifier or title+author, so the receiver's open-free row matching (fp: hash plus author-less ta: slug, per books.py:144-147) will not match every pending record. The accent dot therefore degrades gracefully to absent for some books, and the toast must not promise a resolved title \u2014 use a generic fallback text. With that caveat the fix remains small, draw-only, and high value."
  },
  {
   "device": "covey",
   "value": "med",
   "risk": "low",
   "effort": "small",
   "lens": "cv-ux",
   "title": "Sync overlay wording implies both devices must tap Sync",
   "file": "Project_definition_and_documents/COVEY_RevA_v0.5/firmware/pi/covey-ui/covey_ui/books.py",
   "line": 853,
   "claim": "SyncSession.draw renders 'Syncing... %ds (tap Sync on the others)'. The owner's own notes flag exactly this: receivers park the broadcast with no tap at all; tapping both is only for two-way converge. One-string change to e.g. 'sending my place - others need no tap', eliminating a documented misunderstanding that cost a debugging session.",
   "change": "In /Users/nickhowe/New_phone_project/Project_definition_and_documents/COVEY_RevA_v0.5/firmware/pi/covey-ui/covey_ui/books.py:853, change the format string from \"Syncing... %ds  (tap Sync on the others)\" to \"Syncing... %ds  (sharing my place - receivers need no tap)\". No other code changes.",
   "reasoning": "Verified at books.py:853 \u2014 SyncSession.draw renders \"Syncing... %ds  (tap Sync on the others)\". The code itself contradicts the wording: _send_mesh (line 799) broadcasts unconditionally, the class docstring (771-778) and poll_mesh_background (881+) show mesh receivers park the position with no tap (D-089), and the owner's notes record this exact misunderstanding costing a debugging session. The change is a single display string in draw(); grep shows the string is referenced nowhere else (no tests, no logic), and T.text already constrains width with max_w=r.w-16, so a similar-length replacement cannot overflow the panel. Genuinely small, self-contained UX fix on a working device. Only caveat: the wording must stay accurate for the two-way converge case (pulling the OTHER device's newer position does require a tap there), so avoid an absolute \"no tap needed\" phrasing."
  },
  {
   "device": "covey",
   "value": "med",
   "risk": "low",
   "effort": "medium",
   "lens": "cv-ux",
   "title": "Parked sync record is RAM-only; a covey-ui restart discards it silently",
   "file": "Project_definition_and_documents/COVEY_RevA_v0.5/firmware/pi/covey-ui/covey_ui/books.py",
   "line": 893,
   "claim": "app._book_pending is a plain list on the App object (books.py:893-900); restarting covey-ui.service drops any position that arrived over LoRa before the book was opened. The rec is already a small JSON-safe dict (booksync.make_record) and prefs writes are atomic (prefs.py:72-80), so persisting pending recs under a prefs key and restoring them in App.__init__ is contained. Effort is medium because Peer objects wrap recs and need rehydrating on load.",
   "change": "covey_ui/books.py:893-900 (poll_mesh_background) and :761-768 (_pending_pop): after mutating the pending list, mirror it to prefs as a small JSON list [{\"rec\": p.rec, \"source\": p.source}, ...] under a new key (e.g. \"booksync_pending\") and app.prefs.save(); in App.__init__ (covey_ui/app.py), rehydrate self._book_pending = [booksync.Peer(d[\"rec\"], d[\"source\"]) for d in prefs.get(\"booksync_pending\", [])], optionally dropping entries whose rec[\"t\"] is older than a few days. No change to the offer flow, HMAC verification, or the 8-entry cap.",
   "reasoning": "Claim verified by reading the code. (a) Factually right: app._book_pending is a plain lazily-created list (books.py:893-895) filled by poll_mesh_background (called at 1 Hz from app.py:596-597) and drained only by _pending_pop on book open (books.py:298); it is never persisted, so restarting covey-ui.service silently discards parked LoRa positions \u2014 a trap the project's own notes already flag. The rec is JSON-safe (make_record booksync.py:126-129; unpack_mesh booksync.py:197-198 produce plain str/int/float dicts) and prefs writes are atomic tmp+os.replace and never raise (prefs.py:73-81). (b) Safe on a working device: recs are HMAC-verified before parking, so persisting them adds no trust hole; a restored record only ever produces the existing user-confirmed offer card (JUMP / KEEP MINE, books.py:507-519), so a stale offer is declinable, matching today's in-session behavior; disk writes occur only on the rare park/pop events and the list is capped at 8 entries (books.py:900). Peer rehydration is trivial \u2014 Peer(rec, source) rebuilds everything from the rec (booksync.py:223-231) and heard_at is consumed nowhere in the codebase \u2014 so \"medium\" effort is fair (persist on park and pop, restore in App init, update tests). Recommend adding a max-age check on restore using the rec's t field so a weeks-old position does not resurface, though even without it the offer is harmless because user-confirmed."
  },
  {
   "device": "covey",
   "value": "med",
   "risk": "low",
   "effort": "small",
   "lens": "cv-ux",
   "title": "No now-playing indication on Home while music plays in the background",
   "file": "Project_definition_and_documents/COVEY_RevA_v0.5/firmware/pi/covey-ui/covey_ui/screens.py",
   "line": 169,
   "claim": "Music playback is deliberately ticked from App.update (app.py:583-585) so it continues on any screen, but HomeScreen._tiles hardcodes the Music tile badge to 0 (screens.py:169) \u2014 there is no cue anywhere that music is playing or what track. player.playing/paused flags exist (music.py:168-169). A small accent dot on the Music tile (drawn in HomeScreen.draw where badges are painted, screens.py:266-274) when app.music is playing is a draw-only change.",
   "change": "screens.py:274 (inside HomeScreen.draw, after the badge loop): when tiles[i][0] == \"Music\", do p = getattr(app, \"music\", None); if p is not None and p.playing and not p.paused: pygame.draw.circle(surf, T.ACCENT, (r.x + 22, r.y + 26), 6) \u2014 a small accent dot on the Music tile, using music.py's own playing-and-not-paused predicate so a paused queue does not show as live. No other lines change.",
   "reasoning": "Claim verified in code: app.py:44 creates self.music lazily and app.py:586-588 ticks it from App.update() so playback continues on any screen (the comment at 582-585 states this is deliberate); screens.py:169 hardcodes the Music tile badge to 0 and grep shows no reference to app.music anywhere in screens.py, so Home has no playback cue at all \u2014 the only now-playing strip lives inside the Music screens (music.py:570). The playing/paused flags exist at music.py:168-169 and music.py already uses `p.playing and not p.paused` as its own \"audibly playing\" predicate (lines 580, 703). The implied change is a draw-only addition inside HomeScreen.draw (screens.py:248-276) next to the existing badge painting (266-274): read-only getattr on app.music plus one circle draw, no state mutation, no event handling, naturally guarded by the app.music-is-None case. Genuinely small, self-contained, and safe on a working device."
  },
  {
   "device": "covey",
   "value": "high",
   "risk": "low",
   "effort": "medium",
   "lens": "cv-ux",
   "title": "SCOUT: wallpaper has exactly one per-frame hook point - App.draw_frame's fill",
   "file": "Project_definition_and_documents/COVEY_RevA_v0.5/firmware/pi/covey-ui/covey_ui/app.py",
   "line": 803,
   "claim": "s.fill(T.BG) at app.py:803 is the single per-frame background paint for every screen: screens never fill, they draw T.PANEL rects on top. The only other fills are the locked black frame (app.py:799 \u2014 leave alone), one-shot launch splashes (games.py:811, getapps.py:379), and the reader's cached band (books.py:654 \u2014 the page background, correctly independent). Implementation: load+smoothscale the image once into a 480x800 App-held surface (pattern already in ImageViewScreen, files.py:557-567, including the FS.within_sandbox check) and blit it instead of fill, falling back to fill on any load failure; ~1.5 MB, fine on 512 MB. Honest limitation to design around: status bar, tiles and rows are opaque T.PANEL, so a global wallpaper shows only in gaps; the highest-value/lowest-cost variant is Home-only \u2014 blit at the top of HomeScreen.draw (screens.py:248) \u2014 since per-tile alpha blits at 30 Hz cost real time on this Pi (present() alone is 32 ms).",
   "change": "Home-only wallpaper: load the image once at App init using the files.py:557-567 pattern (FS.within_sandbox check, pygame.image.load, smoothscale to 480x800, exceptions caught leaving the surface None), hold it on App, and blit it as the first operation of HomeScreen.draw (screens.py:248) when non-None. Do not modify App.draw_frame \u2014 its s.fill(T.BG) at app.py:806 stays and is the automatic fallback whenever the wallpaper is absent or failed to load. Correct the finding's citations to app.py:806 (T.BG fill) and app.py:802 (locked black fill).",
   "reasoning": "Claim verified by reading the code, with a 3-line citation drift: the per-frame background fill s.fill(T.BG) is at app.py:806 (not 803) and the locked black fill at app.py:802 (not 799), inside App.draw_frame as claimed. Screens genuinely never fill the background: HomeScreen.draw (screens.py:248) draws only T.PANEL rects; the full grep of .fill( across covey_ui confirms every other fill is either one-shot (games.py:811/846, getapps.py:379), the reader's independent page band (books.py:654), the 320x240 Lua-app sub-surface (luahost.py:224/229/281, size at :34), a missing-map-tile placeholder (mapview.py:112), or black overlay veils. The cited ImageViewScreen load pattern (files.py:557-567 with FS.within_sandbox, pygame.image.load, smoothscale, caught errors) exists exactly as described, screen is 480x800 (theme.py:4), and the ~32 ms present figure comes from the app's own comment (app.py:771). The finding's honest limitation (opaque T.PANEL panels hide a global wallpaper; Home-only is the sensible variant) is accurate. Safety: the Home-only variant is one blit at the top of HomeScreen.draw with an image loaded once at startup; since draw_frame fills T.BG before scr.draw, any load failure degrades to today's exact behavior automatically \u2014 no failure-path code, nothing touches locked/reader/games/Lua paths. Small, self-contained, low risk on a working device."
  }
 ],
 "rejected": 2
}